import 'package:flutter/material.dart';

class MultiChannelWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text('Đa kênh', style: TextStyle(fontSize: 24)),
      ),
    );
  }
} 